name: material-tailwind-dashboard-react
region: us-east-1
frontend:

  path: .
yed.
  publish: dist

  scripts:

    deploy:
      - npm install --legacy-peer-deps
      - npm run build
yamlVersion: 2